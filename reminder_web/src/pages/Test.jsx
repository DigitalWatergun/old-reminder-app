import React from "react"; 
import { HeaderFooter } from "../components/HeaderFooter";

export const Test = () => {
    return (
        <HeaderFooter>
            test
        </HeaderFooter>
    )
}